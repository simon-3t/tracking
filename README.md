# tracking — Ingestion & P&L crypto (Binance)

## Installation
```bash
git clone https://github.com/simon-3t/tracking.git
cd tracking
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# édite .env et mets tes clés (lecture seule)
